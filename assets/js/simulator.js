// Minimal simulator.js - Schritt für Schritt aufbauen
// Version: 1.0 - Nur das Nötigste

jQuery(document).ready(function($) {
    
    console.log('✅ Minimal Simulator.js geladen');
    console.log('✅ User eingeloggt:', luvexAjax.is_user_logged_in);
    console.log('✅ REST URL verfügbar:', luvexAjax.resturl);
    
    // Mehr Funktionen werden wir hier Schritt für Schritt hinzufügen
    // wenn wir sie brauchen und verstehen, was sie tun
    
});